// file: largebytes/app/_components/utils/SectionTitle.tsx

const SectionTitle = ({ children }: { children: string }) => {
  return <h2 className="text-2xl font-bold my-8 leading-relaxed">{children}</h2>;
};

export default SectionTitle;
